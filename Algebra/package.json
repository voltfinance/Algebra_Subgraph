{
  "name": "algebra-subgraph",
  "version": "1.0.0",
  "repository": "https://github.com/graphprotocol/algebra-subgraph",
  "license": "GPL-3.0-or-later",
  "scripts": {
    "codegen": "graph codegen --output-dir src/types/",
    "build": "graph build",
    "create-local": "graph create --node http://localhost:8020/ algebra",
    "deploy-local": "graph deploy --debug --node http://localhost:8020/ algebra",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ algebra",
    "create": "graph create algebra --node https://api.thegraph.com/create/"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.91.1",
    "@graphprotocol/graph-ts": "0.36.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}